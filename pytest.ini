[pytest]
addopts = -vs
    # --alluredir=./temp
    # --clean-alluredir
    # --reruns 2
log_cli = 0
log_file = ./logs/test.log
log_file_level = info
log_file_foramt = %(levelname)-8s %(asctime)s [%(name)s:%(lineno)s] : %(message)s
log_file_date_format = %Y-%m-%d %H:%M:%S
